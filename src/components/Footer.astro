---
---

<footer>
  <small>Design and develop by Jech</small>
</footer>

<style lang="scss">
  footer {
    display: flex;
    width: 100%;
    align-items: flex-end;
    justify-content: flex-end;
    padding-block: 1rem;
    max-width: var(--w-full);
    margin-inline: auto;
    > small {
      opacity: 0.4;
    }
  }
</style>
<script type="module" src="/app.js"></script>
<!-- <script defer>
  window.onload = () => {
    const anchors = document.querySelectorAll("a");
    const transition_el = document.querySelector(".anim");

    setTimeout(() => {
      transition_el.classList.remove("blur");
    }, 500);

    for (let i = 0; i < anchors.length; i++) {
      const anchor = anchors[i];

      anchor.addEventListener("click", (e) => {
        e.preventDefault();
        let target = e.target;

        if (target.classList.contains("animTrigger")) {
          transition_el.classList.add("blur");

          setInterval(() => {
            window.location.href = target;
          }, 500);
        } else {
          window.location.href = target;
        }
      });
    }
  };
</script> -->
